logging:
  level:
    javax:
      persistence: trace
    org:
      hibernate:
        SQL: debug
        type:
          descriptor:
            sql: trace
      springframework:
        orm:
          jpa: debug
        security: debug
        transaction: trace
    root: debug